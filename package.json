{
  "name": "mex-backend",
  "version": "1.0.0",
  "devDependencies": {
    "serverless-add-api-key": "^4.2.1",
    "serverless-domain-manager": "~6.2.2",
    "serverless-offline": "~12.0.4",
    "serverless-prune-plugin": "^2.0.1",
    "serverless-simple-alias": "^1.0.0"
  },
  "dependencies": {
    "serverless": "3.27.0"
  },
  "scripts": {
    "deploy:test": "mvn clean install && sls deploy --stage test --verbose",
    "deploy:staging": "mvn clean install && sls deploy --stage staging --verbose"
  }
}
