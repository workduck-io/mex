Resources:
  DDBStreamSNS:
    Type: AWS::SNS::Topic
    Properties:
      DisplayName: 'dlq-sns-mex'
      TopicName: dlq-sns-${opt:stage, self:provider.stage}
      Subscription:
       - Endpoint: "varun.garg@workduck.io"
         Protocol: "email"
  DDBAlarmsSNS:
    Type: AWS::SNS::Topic
    Properties:
      DisplayName: 'ddb-alarms-mex'
      TopicName: ddb-alarms-mex-${opt:stage, self:provider.stage}
      Subscription:
        - Endpoint: "ddb-alarms-aaaaiul5sicbiorxwhkybvam4a@workduck-workspace.slack.com"
          Protocol: "email"